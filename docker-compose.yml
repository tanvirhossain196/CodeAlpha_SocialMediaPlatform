services:
  postgres:
    image: postgres:16-alpine
    container_name: connectly-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: connectly
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "55432:5432"
    volumes:
      - connectly_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d connectly"]
      interval: 5s
      timeout: 5s
      retries: 10
volumes:
  connectly_pgdata:

