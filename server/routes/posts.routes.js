const router=require('express').Router();const c=require('../controllers/posts.controller');const comments=require('../controllers/comments.controller');const {requireAuth}=require('../middleware/auth');const upload=require('../middleware/upload');
router.get('/',requireAuth,c.list);router.get('/user/:userId',requireAuth,c.byUser);router.get('/:id',requireAuth,c.getOne);router.post('/',requireAuth,upload.single('image'),c.create);router.put('/:id',requireAuth,c.update);router.delete('/:id',requireAuth,c.remove);router.post('/:id/like',requireAuth,c.like);router.delete('/:id/like',requireAuth,c.unlike);router.get('/:id/comments',requireAuth,comments.list);router.post('/:id/comments',requireAuth,comments.create);module.exports=router;
