const router=require('express').Router();const c=require('../controllers/users.controller');const {requireAuth,optionalAuth}=require('../middleware/auth');const upload=require('../middleware/upload');
router.get('/search',requireAuth,c.searchUsers);router.get('/:id',optionalAuth,c.getUser);router.put('/:id',requireAuth,upload.single('avatar'),c.updateUser);router.post('/:id/follow',requireAuth,c.follow);router.delete('/:id/follow',requireAuth,c.unfollow);router.get('/:id/followers',requireAuth,c.followers);router.get('/:id/following',requireAuth,c.following);module.exports=router;
