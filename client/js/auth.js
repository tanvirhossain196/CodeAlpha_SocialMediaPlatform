function showFieldErrors(form,errors={}){form.querySelectorAll('.field-error').forEach(e=>e.textContent='');form.querySelectorAll('.input').forEach(e=>e.classList.remove('error'));Object.entries(errors).forEach(([k,v])=>{const input=form.querySelector(`[name="${k}"]`);const out=form.querySelector(`[data-error="${k}"]`);input?.classList.add('error');if(out)out.textContent=v})}
async function submitAuth(form,type){const btn=form.querySelector('button[type="submit"]');showFieldErrors(form);btn.disabled=true;btn.textContent=type==='login'?'Signing in…':'Creating account…';try{const raw=Object.fromEntries(new FormData(form));if(type==='register'&&raw.password!==raw.confirmPassword){showFieldErrors(form,{confirmPassword:'Passwords do not match.'});return}const body=type==='login'?{identity:raw.identity,password:raw.password,remember:raw.remember==='on'}:{fullName:raw.fullName,username:raw.username,email:raw.email,password:raw.password};const d=await API.post(`/auth/${type}`,body);toast(d.message,'success');location.href='/home.html'}catch(e){showFieldErrors(form,e.errors);toast(e.message,'error')}finally{btn.disabled=false;btn.textContent=type==='login'?'Sign in':'Create account'}}
document.querySelector('#loginForm')?.addEventListener('submit',e=>{e.preventDefault();submitAuth(e.currentTarget,'login')});document.querySelector('#registerForm')?.addEventListener('submit',e=>{e.preventDefault();submitAuth(e.currentTarget,'register')});document.querySelectorAll('.password-toggle').forEach(b=>b.addEventListener('click',()=>{const i=b.parentElement.querySelector('input');i.type=i.type==='password'?'text':'password';b.textContent=i.type==='password'?'Show':'Hide'}));
